# ** do not modify **
train_split: 0.9
data_path: "./mnist.npz"

hydra:  
  output_subdir: null  
  run:  
    dir: .

# ** can modify **

network_size: [0, 0, 0]

epochs: 0
lr: 0
decay: 0
batch_size: 0
